#include "BasicModule.h"

void BasicModule::ex_thread_process(void) {	// implementation of member function (process)
	for (int i=3; i>0; i--) {		// display an output that could count down from 3 to 1
		cout << i << endl;
	}
}

